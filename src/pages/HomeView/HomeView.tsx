import './HomeView.css';

const HomeView = (): React.ReactElement => {
    return (
        <div className="home-view-container">
            <p>HOME</p>
        </div>
    );
};

export default HomeView;
